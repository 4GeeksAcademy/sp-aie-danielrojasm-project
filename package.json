{
  "name": "trackflow-public-site",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page publica de TrackFlow",
  "scripts": {
    "dev": "npx serve . -l 4173",
    "check:ts": "npx -y tsc --noEmit -p tsconfig.json",
    "run:ts": "npx -y tsx"
  },
  "dependencies": {
    "typescript": "^6.0.3"
  }
}
